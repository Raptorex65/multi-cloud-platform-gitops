apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: test-secret
  namespace: microservices-dev

spec:
  refreshInterval: 1m

  secretStoreRef:
    name: azure-keyvault-store
    kind: SecretStore

  target:
    name: app-secret

  data:
    - secretKey: password
      remoteRef:
        key: test-secret
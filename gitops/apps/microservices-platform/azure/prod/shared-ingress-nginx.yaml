apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-ingress-prod
  namespace: microservices-prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api-service
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-dev-v2-frontend
                port:
                  number: 80
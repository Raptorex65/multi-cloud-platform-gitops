apiVersion: v1
kind: Service
metadata:
 name: api-service
spec:
 selector:
  app: {{ include "api.fullname" . }}
 ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
 type: ClusterIP      